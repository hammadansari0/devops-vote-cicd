- hosts: all
  become: true
  roles:
    - docker
    - kubernetes-docker

- hosts: k8s_master
  become: true
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  roles:
    - master
    - helm
    - monitoring
    - jenkins

- hosts: k8s_workers
  become: true
  roles:
    - worker